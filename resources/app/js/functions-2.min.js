var keyboardType=document.getElementById("keyboard-type"),testButton=document.getElementById("test-button");testButton.addEventListener("click",()=>{let type,url="../keyboard-layouts/"+keyboardType.value.toLowerCase().trim().replace(/\s/g,"-").replace(/[^a-zA-Z-]/g,"")+".html";location.assign(url)});